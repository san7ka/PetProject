# PetProject

Проект Java (Gradle) — создан в IntelliJ IDEA.

Структура:
- `app/` — модуль приложения
- `src/` — исходники (если есть)

Инструкции:

```bash
# Сборка
./gradlew build

# Запуск (если есть приложение)
./gradlew run
```

